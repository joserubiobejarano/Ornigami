"use client";

import { Suspense, useState } from "react";
import Image from "next/image";
import Link from "next/link";
import { useSearchParams } from "next/navigation";
import { signIn } from "next-auth/react";

import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { Label } from "@/components/ui/label";
import { safeRelativeRedirect } from "@/lib/safe-redirect";

function LoginForm() { const searchParams = useSearchParams(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState<string | null>(null); const [isSubmitting, setIsSubmitting] = useState(false); const afterLoginUrl = safeRelativeRedirect(searchParams?.get("callbackUrl") ?? undefined, "/dashboard"); async function onSubmit(e: React.FormEvent) { e.preventDefault(); setError(null); if (!email) { setError("Enter your email address."); return; } if (!password) { setError("Enter your password."); return; } setIsSubmitting(true); try { const res = await signIn("credentials", { email, password, redirect: false }); if (res?.error) { setError("That email or password doesn't match. Try again or reset your password."); return; } window.location.href = afterLoginUrl; } catch { setError("That email or password doesn't match. Try again or reset your password."); } finally { setIsSubmitting(false); } } async function signInWithGoogle() { await signIn("google", { callbackUrl: afterLoginUrl }); } return <main className="flex min-h-dvh items-center justify-center bg-surface/55 p-6"><form onSubmit={onSubmit} className="w-full max-w-md space-y-5 rounded-3xl border-[1.5px] border-border bg-card p-8 shadow-ink-md"><Link href="/" className="inline-flex"><Image src="/logo-ink.svg" alt="Ornigami" width={180} height={72} className="h-10 w-[150px] object-contain object-left dark:hidden" /><Image src="/logo-paper.svg" alt="Ornigami" width={180} height={72} className="hidden h-10 w-[150px] object-contain object-left dark:block" /></Link><div><span className="inline-flex rounded-full border border-border bg-tint-navy px-3 py-1 text-[0.7rem] font-bold uppercase tracking-[0.12em] text-primary">Welcome back</span><h1 className="mt-4 text-3xl font-extrabold text-primary">Welcome back.</h1></div><Button type="button" variant="secondary" onClick={signInWithGoogle} className="w-full">Continue with Google</Button><div className="flex items-center gap-3 text-xs text-muted-foreground"><span className="h-px flex-1 bg-border" />or<span className="h-px flex-1 bg-border" /></div><div className="space-y-2"><Label htmlFor="login-email">Email</Label><Input id="login-email" type="email" autoComplete="email" value={email} onChange={(e) => setEmail(e.target.value)} /></div><div className="space-y-2"><Label htmlFor="login-password">Password</Label><Input id="login-password" type="password" autoComplete="current-password" value={password} onChange={(e) => setPassword(e.target.value)} /></div>{error ? <p role="alert" className="rounded-xl border-[1.5px] border-destructive/30 bg-destructive/10 px-3 py-2 text-sm text-destructive">{error}</p> : null}<Button type="submit" className="w-full" disabled={isSubmitting}>{isSubmitting ? "Logging in…" : "Log in"}</Button><p className="text-right text-sm"><Link className="text-primary underline underline-offset-4" href="/contact?subject=password-reset">Forgot password?</Link></p><p className="text-sm text-muted-foreground">New here? <Link className="font-semibold text-primary underline underline-offset-4" href="/signup">Start free trial</Link></p></form></main>; }

export default function LoginPage() { return <Suspense fallback={<main className="flex min-h-dvh items-center justify-center p-6"><p className="text-muted-foreground">Loading…</p></main>}><LoginForm /></Suspense>; }
